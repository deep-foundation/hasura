{
  "name": "hasura-example",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "dev": "next dev -p $PORT",
    "build": "next build",
    "export": "next export",
    "start": "next start",
    "gh-pages": "npm run build; npm run export; rm -rf ../hasura-gh-pages/*; mv ./out/* ../hasura-gh-pages/; rm -rf out",
    "migrate": "export $(grep -v '^#' .env | xargs); yes | DEBUG='*' npx migrate up --compiler='ts:@deepcase/hasura/compiler/index.js'",
    "unmigrate": "export $(grep -v '^#' .env | xargs); yes | DEBUG='*' npx migrate down --compiler='ts:@deepcase/hasura/compiler/index.js'",
    "gitpod": "export $(grep -v '^#' .env | xargs); export MIGRATIONS_HASURA_PATH=localhost:8080; export MIGRATIONS_HASURA_SSL=0; export MIGRATIONS_HASURA_SECRET=myadminsecretkey; MIGRATIONS_EXAMPLE_URL=$(echo $(gp url 3001) | awk -F[/:] '{print $4}'); export HASURA_PATH=$(echo $(gp url 8080) | awk -F[/:] '{print $4}'); export HASURA_SSL=1; export PORT=3001; npx next dev -p $PORT"
  },
  "dependencies": {
    "@deepcase/hasura": "^0.0.6",
    "@deepcase/react-hasura": "^0.0.1",
    "@deepcase/store": "^0.0.31",
    "apollo-boost": "^0.4.9",
    "apollo-server": "^2.21.0",
    "dotenv": "^8.2.0",
    "dotenv-webpack": "^6.0.2",
    "graphql-tag": "^2.11.0",
    "next": "10.0.7",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@types/react": "^17.0.2"
  }
}
